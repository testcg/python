作业
1. 三合一
2. 当天练习独立完成
3. 为IterableHelper添加新功能
需求：
    定义函数,删除薪资大于2w的所有员工
    定义函数,删除部门编号是9001的所有员工
步骤：
    1. 根据需求，写出函数。
    2. 因为主体逻辑相同,核心算法不同.
       创建通用函数delete_all
       定义在IterableHelper中
    3. 使用lambda在当前模块中调用

需求：
    定义函数,在员工列表中获取薪资最高的员工
    定义函数,在员工列表中获取员工编号最大的员工
步骤：
    1. 根据需求，写出函数。
    2. 因为主体逻辑相同,核心算法不同.
       创建通用函数get_max
       定义在IterableHelper中
    3. 使用lambda在当前模块中调用
注意：只完成获取最大,不负责最小

需求：
    定义函数,根据薪资对员工列表进行升序排列
    定义函数,根据编号对员工列表进行降序排列
步骤：
    1.在IterableHelper中创建
       通用函数 sort
    2. 使用lambda在当前模块中调用
注意：不要返回值
